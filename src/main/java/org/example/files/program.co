define domain robotDomain

         type robot {
             carrying: boolean;
         }

         action move(robot r, room d) {
             r.missingField = d;
         }
 define problem p
        import robotDomain

        objects {
            robot robots[] = {rob};
            room rooms[] = {A};
        }

        initialState {
            rob.carrying = false;
        }

        goalState {
            rob.carrying == true;
        }