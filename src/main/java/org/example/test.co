define domain myDomain
type robot {
    speed: int;
    location: room || robot || door;
    amalie: int[];
}

type room { }
type door { }

type package {
    location: room;
    speed: int;
}

action robot(robot r) {
    if (r.location == room) {
        r.speed = door;
    }
}

action pickUp(robot r, package p) {
    if (r.location == p.location) {
        p.location = r;
    }
}

define problem p1
import myDomain
objects {
    robot robots[] = {r1, r2, r3};
    package bluePackages[] = {p1, p2};
    package redPackages[] = {pa1, pa2};
}
initialState {
    robots[0, 1].location = A;
}

goalState {
    r1.location == B;
}